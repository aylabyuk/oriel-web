export {
  themeSlice,
  setEnvironment,
  setReducedMotion,
  selectEnvironment,
  selectReducedMotion,
} from '@/store/slices/theme/themeSlice';

export type { EnvironmentPreset } from '@/store/slices/theme/themeSlice';
